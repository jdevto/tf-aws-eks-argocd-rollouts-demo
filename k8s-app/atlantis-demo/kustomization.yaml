apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  # secret.yaml is excluded - create via kubectl or External Secrets
  # See README.md for instructions
  - rollout.yaml
  - service.yaml
  - service-preview.yaml
  - ingress.yaml
